{
  "name": "goapp",
  "version": "1.0.0",
  "description": "This project assumes you have go and npm installed",
  "main": "server.go",
  "scripts": {
    "js": "cp assets/js/*.js public",
    "files": "cp assets/favicon.ico public && cp assets/robots.txt public && cp assets/noext public && cp -r assets/images public",
    "sass": "node-sass assets/scss --output public",
    "assets" : "npm run js && npm run sass && npm run files"
  },
  "author": "Julian Chan",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.5.2"
  }
}
