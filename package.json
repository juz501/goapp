{
  "name": "goapp",
  "version": "1.0.0",
  "description": "This project assumes you have go and npm installed",
  "main": "server.go",
  "scripts": {
    "install": "npm run install-negroni && npm run install-render",
    "install-negroni": "sh -c 'GOPATH=`pwd -P` go get github.com/urfave/negroni'",
    "install-render": "sh -c 'GOPATH=`pwd -P` go get github.com/unrolled/render'",
    "start": "sh -c 'GOPATH=`pwd -P` PORT=8080 go run server.go'",
    "js": "cp assets/js/*.js public",
    "files": "cp assets/favicon.ico public && cp assets/robots.txt public",
    "sass": "node-sass assets/scss --output public"
  },
  "author": "Julian Chan",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.5.2"
  }
}
